{% extends 'base.html.twig' %}

{% block title %}Mon compte - Luc'rousty{% endblock %}

{% block content %}
    <h1>Mon compte</h1>
    {% if is_granted('ROLE_ADMIN') %}
       <small><a href="/admin" class="go-backoffice">Accéder au backoffice</a></small> <br>
    {% endif %}
    Bienvenue {{ app.user.firstname }} dans votre compte ! <br/>
    C'est dans cet espace que vous allez pouvoir gérer vos informations personnelles.
    <hr>

    <div class="row">
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/password.png') }}" class="mb-3" width="32px"/><br>
                <a href="{{ path('account_password') }}">Modifier mon mot de passe</a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/address-book.png') }}" class="mb-3" width="32px"/><br>
                <a href="{{ path('account_address') }}">Gérer mes adresses</a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/order.png') }}" class="mb-3" width="32px"/><br>
                <a href="{{ path('account_order') }}">Mes commandes</a>
            </div>
        </div>
    </div>

{% endblock %}
